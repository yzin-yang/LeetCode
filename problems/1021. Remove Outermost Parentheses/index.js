/**
 * @param {string} S
 * @return {string}
 */
var removeOuterParentheses = function (S) {
    let str = [];
    let deleteStr = [];
    for (let i = 0; i < S.length; i++) {
        if (S.charAt(i)===')'&&str.length>1)

    }
};